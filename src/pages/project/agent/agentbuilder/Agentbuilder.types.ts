// Generated types for Agentbuilder
// Note: Basic data types (User, etc.) are defined in @/data

// Page-specific types for Agentbuilder
export interface AgentbuilderPageState {
    isLoading: boolean;
    error: string | null;
    // Add page-specific state types here
}

export type MenuBarProps = object;

export type BoxProps = object;

export type AgentbuilderProps = object;
