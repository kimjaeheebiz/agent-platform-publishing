// Generated types for Credential
// Note: Basic data types (User, etc.) are defined in @/data

// Page-specific types for Credential
export interface CredentialPageState {
    isLoading: boolean;
    error: string | null;
    // Add page-specific state types here
}

export type ContentProps = object;

export type CredentialProps = object;
