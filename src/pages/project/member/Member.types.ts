// Generated types for Member
// Note: Basic data types (User, etc.) are defined in @/data

// Page-specific types for Member
export interface MemberPageState {
    isLoading: boolean;
    error: string | null;
    // Add page-specific state types here
}

export type ContentProps = object;

export type MemberProps = object;
