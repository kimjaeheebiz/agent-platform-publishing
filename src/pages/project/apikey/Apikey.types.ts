// Generated types for Apikey
// Note: Basic data types (User, etc.) are defined in @/data

// Page-specific types for Apikey
export interface ApikeyPageState {
    isLoading: boolean;
    error: string | null;
    // Add page-specific state types here
}

export type BoxProps = object;

export type ApikeyProps = object;
