// Generated types for Document
// Note: Basic data types (User, etc.) are defined in @/data

// Page-specific types for Document
export interface DocumentPageState {
    isLoading: boolean;
    error: string | null;
    // Add page-specific state types here
}

export type ContentProps = object;

export type DocumentProps = object;
