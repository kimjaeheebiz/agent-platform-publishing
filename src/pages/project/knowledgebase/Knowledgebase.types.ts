// Generated types for Knowledgebase
// Note: Basic data types (User, etc.) are defined in @/data

// Page-specific types for Knowledgebase
export interface KnowledgebasePageState {
    isLoading: boolean;
    error: string | null;
    // Add page-specific state types here
}

export type ContentProps = object;

export type KnowledgebaseProps = object;
