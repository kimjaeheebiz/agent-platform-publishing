// Generated types for Recycle
// Note: Basic data types (User, etc.) are defined in @/data

// Page-specific types for Recycle
export interface RecyclePageState {
    isLoading: boolean;
    error: string | null;
    // Add page-specific state types here
}

export type BoxProps = object;

export type RecycleProps = object;
