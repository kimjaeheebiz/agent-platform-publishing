// Generated types for Agent
// Note: Basic data types (User, etc.) are defined in @/data

// Page-specific types for Agent
export interface AgentPageState {
    isLoading: boolean;
    error: string | null;
    // Add page-specific state types here
}

export type ContentProps = object;

export type AgentProps = object;
