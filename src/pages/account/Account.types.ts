// Generated types for Account
// Note: Basic data types (User, etc.) are defined in @/data

// Page-specific types for Account
export interface AccountPageState {
    isLoading: boolean;
    error: string | null;
    // Add page-specific state types here
}

export type RowProps = object;

export type AccountProps = object;
